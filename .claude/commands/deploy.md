앱인토스용 .ait 번들을 빌드하고 배포합니다.

1. `npx ait build`를 실행하여 .ait 번들을 생성합니다.
2. 빌드 결과(성공/실패)를 확인합니다.
3. 성공 시 생성된 .ait 파일 경로와 크기를 알려줍니다.
4. 사용자에게 안내: "앱인토스 콘솔(https://apps-in-toss.toss.im/)에서 앱 출시 > 번들 파일 올리고 테스트하기에서 업로드하세요"
5. 배포 명령도 실행할지 물어봅니다: `npx ait deploy`
